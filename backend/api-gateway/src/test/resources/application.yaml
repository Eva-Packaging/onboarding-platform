spring:
  cloud:
    consul:
      enabled: false
      discovery:
        enabled: false
    gateway:
      server:
        webflux:
          discovery:
            locator:
              enabled: false