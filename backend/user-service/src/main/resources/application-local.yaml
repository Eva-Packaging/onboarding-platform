server:
  port: 0
spring:
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        enabled: false
logging:
  level:
    root: info
    xyz:
      catuns:
        onboarding:
          user: debug